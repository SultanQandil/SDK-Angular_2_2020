import { AppSirectiveDirective } from './app-sirective.directive';

describe('AppSirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
